import os# การกำหนดสีBLUE = '\033[94m'     # สีฟ้า (สำหรับข้อความกระบวนการ)GREEN = '\033[92m'    # สีเขียว (สำหรับสำเร็จ)RED = '\033[91m'      # สีแดง (สำหรับล้มเหลว)RESET = '\033[0m'     # รีเซ็ตสีกลับสู่ค่าเดิม# เพิ่มเครดิตprint(f"{BLUE}--- ZER™X NET ---{RESET}")print(f"{BLUE}โปรแกรมนี้ถูกพัฒนาโดย ZERO™X NET{RESET}")print(f"{BLUE}เวอร์ชันนี้ใช้สำหรับสแกน IP และตรวจสอบการเชื่อมต่อด้วย Ping{RESET}")print("-" * 50)# รับค่าจากผู้ใช้สำหรับช่วง IPbase_ip = input("กรุณากรอก IP ที่ต้องการเริ่มต้น (เช่น 104.18.62.): ")start_ip = int(input("กรุณากรอกหมายเลข IP เริ่มต้น (0-255): "))end_ip = int(input("กรุณากรอกหมายเลข IP สิ้นสุด (0-255): "))# ลูปทำการ ping IP ในช่วงที่กรอกfor i in range(start_ip, end_ip + 1):    ip = f"{base_ip}{i}"  # สร้าง IP โดยไม่ใส่ศูนย์นำหน้า    print(f"{BLUE}กำลัง Ping {ip}...{RESET}", flush=True)  # สีฟ้าสำหรับข้อความกระบวนการ    response = os.system(f"ping -c 1 {ip} > /dev/null 2>&1")  # ปิดการแสดงผลการ ping    if response == 0:        print(f"{GREEN}{ip} pinged successful ✓{RESET}", flush=True)  # สีเขียวสำหรับสำเร็จ    else:        print(f"{RED}{ip} pinged failed ✗{RESET}", flush=True)  # สีแดงสำหรับล้มเหลว